[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bun run src/server.ts"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Runs are held in memory, so a second instance would serve half the seats.
numReplicas = 1
